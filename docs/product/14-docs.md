# Documentation

This portal is a generated public snapshot for technical due diligence. It is deliberately not a dump of a private Obsidian vault or runtime directory.

## Source and drift

Reviewed Markdown in this directory is converted into typed site data. QA checks the manifest, generated output, routes, public-content boundary, and proof data.

- Obsidian remains the human-readable source of truth for the installed system.
- Public docs are a curated projection with stable URLs.
- Generated output is checked for drift before release.
